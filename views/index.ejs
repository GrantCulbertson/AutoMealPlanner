<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/public/styles.css" rel="stylesheet">
</head>
<body class="bg-body">
  <nav class="navbar navbar-expand-lg navbar-dark bg-darkgray mb-4">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/">AutoMeal Planner</a>
      <div>
        <a class="btn btn-outline-light" href="/plans">View Plans</a>
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="row justify-content-center">
      <div class="col-lg-10 col-xl-8">
        <div class="card shadow-sm">
          <div class="card-header bg-sage text-white">
            <h5 class="mb-0">Create your weekly meal plan</h5>
          </div>
          <div class="card-body">
            <% if (message) { %>
              <div class="alert alert-danger"><%= message %></div>
            <% } %>
            <form action="/generate" method="POST" class="row g-3">
              <div class="col-md-6">
                <label class="form-label">Where do you live?</label>
                <input type="text" name="location" class="form-control" placeholder="City, State or Country">
              </div>
              <div class="col-md-6">
                <label class="form-label">Primary grocery store</label>
                <input type="text" name="groceryStore" class="form-control" placeholder="e.g., Trader Joe's">
              </div>
              <div class="col-md-6">
                <label class="form-label">Weekly grocery budget (USD)</label>
                <input type="number" step="0.01" name="weeklyBudgetUsd" class="form-control" placeholder="e.g., 100">
              </div>
              <div class="col-md-6">
                <label class="form-label">Meals you want plans for</label>
                <input type="text" name="mealsRequested" class="form-control" placeholder="e.g., high-protein, vegetarian, low-carb">
              </div>
              <div class="col-12">
                <label class="form-label">Tools and pans you have</label>
                <input type="text" name="kitchenTools" class="form-control" placeholder="e.g., oven, air fryer, instant pot">
              </div>
              <div class="col-12">
                <label class="form-label">Food categories you like</label>
                <input type="text" name="favoriteCategories" class="form-control" placeholder="e.g., Mediterranean, Mexican, Asian, salads">
              </div>
              <div class="col-md-6">
                <label class="form-label">Recipe complexity</label>
                <input type="range" min="1" max="5" value="3" name="complexity" class="form-range">
                <div class="form-text">1 = very simple, 5 = gourmet</div>
              </div>
              <div class="col-12 d-flex justify-content-end">
                <button type="submit" class="btn btn-sage">Generate Plan</button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>

  <footer class="text-center mt-5 mb-4 text-muted small">
    Built with ❤️ using Node.js, Express, and EJS
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

