<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/public/styles.css" rel="stylesheet">
</head>
<body class="bg-body">
  <nav class="navbar navbar-expand-lg navbar-dark bg-darkgray mb-4">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/">AutoMeal Planner</a>
      <div>
        <a class="btn btn-outline-light me-2" href="/">Home</a>
        <a class="btn btn-outline-light" href="/plans">View Plans</a>
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="row justify-content-center">
      <div class="col-lg-10 col-xl-8">
        <div class="card shadow-sm">
          <div class="card-header bg-sage text-white">
            <h5 class="mb-0">Kitchen Profile</h5>
          </div>
          <div class="card-body">
            <% if (message) { %>
              <div class="alert alert-success"><%= message %></div>
            <% } %>
            <form action="/profile" method="POST" class="row g-3">
              <div class="col-md-6">
                <label class="form-label">Where do you live? <span class="text-danger">*</span></label>
                <input type="text" name="location" class="form-control" placeholder="City, State or Country" value="<%= profile.location || '' %>" required>
              </div>
              <div class="col-md-6">
                <label class="form-label">Primary grocery store <span class="text-danger">*</span></label>
                <input type="text" name="groceryStore" class="form-control" placeholder="e.g., Trader Joe's" value="<%= profile.groceryStore || '' %>" required>
              </div>
              <div class="col-md-6">
                <label class="form-label">Weekly grocery budget (USD) <span class="text-danger">*</span></label>
                <input type="number" step="0.01" name="weeklyBudgetUsd" class="form-control" placeholder="e.g., 100" value="<%= profile.weeklyBudgetUsd || '' %>" required>
              </div>
              <div class="col-md-6">
                <label class="form-label">Meals you want plans for</label>
                <input type="text" name="mealsRequested" class="form-control" placeholder="e.g., high-protein, vegetarian, low-carb" value="<%= profile.mealsRequested || '' %>">
              </div>

              <div class="col-12">
                <label class="form-label">Food categories you like</label>
                <input type="text" name="favoriteCategories" class="form-control" placeholder="e.g., Mediterranean, Mexican, Asian, salads" value="<%= profile.favoriteCategories || '' %>">
              </div>
              <div class="col-md-6">
                <label class="form-label">Recipe complexity <span id="complexity-value">(<%= profile.complexity || 3 %>)</span></label>
                <input type="range" min="1" max="5" value="<%= profile.complexity || 3 %>" name="complexity" class="form-range" id="complexity-slider" oninput="updateComplexityValue(this.value)">
                <div class="form-text">1 = very simple, 5 = gourmet</div>
              </div>
              <div class="col-12">
                <label class="form-label">Kitchen Tools & Pans</label>
                <textarea name="kitchenTools" class="form-control" rows="3" placeholder="e.g., cast iron skillet, non-stick pan, wok, baking sheets, mixing bowls"><%= profile.kitchenTools || '' %></textarea>
                <div class="form-text">List the cooking tools and pans you have available</div>
              </div>
              <div class="col-12">
                <label class="form-label">Kitchen Appliances & Utilities</label>
                <textarea name="kitchenAppliances" class="form-control" rows="3" placeholder="e.g., oven, stovetop, microwave, air fryer, instant pot, rice cooker, blender"><%= profile.kitchenAppliances || '' %></textarea>
                <div class="form-text">List the appliances and utilities in your kitchen</div>
              </div>
              <div class="col-12">
                <label class="form-label">Cooking Preferences & Notes</label>
                <textarea name="cookingNotes" class="form-control" rows="2" placeholder="e.g., prefer quick meals, love spicy food, avoid dairy"><%= profile.cookingNotes || '' %></textarea>
                <div class="form-text">Any additional cooking preferences or dietary notes</div>
              </div>
              <div class="col-12 d-flex justify-content-end">
                <button type="submit" class="btn btn-sage">Save Profile</button>
              </div>
            </form>
          </div>
        </div>
        
        <% if (profile.location || profile.groceryStore || profile.weeklyBudgetUsd || profile.mealsRequested || profile.favoriteCategories || profile.complexity || profile.kitchenTools || profile.kitchenAppliances || profile.cookingNotes) { %>
        <div class="card shadow-sm mt-4">
          <div class="card-header bg-light">
            <h6 class="mb-0">Current Profile</h6>
          </div>
          <div class="card-body">
            <% if (profile.location) { %>
            <div class="mb-3">
              <strong>Location:</strong>
              <p class="mb-0"><%= profile.location %></p>
            </div>
            <% } %>
            <% if (profile.groceryStore) { %>
            <div class="mb-3">
              <strong>Grocery Store:</strong>
              <p class="mb-0"><%= profile.groceryStore %></p>
            </div>
            <% } %>
            <% if (profile.weeklyBudgetUsd) { %>
            <div class="mb-3">
              <strong>Weekly Budget:</strong>
              <p class="mb-0">$<%= profile.weeklyBudgetUsd %></p>
            </div>
            <% } %>
            <% if (profile.mealsRequested) { %>
            <div class="mb-3">
              <strong>Meals Requested:</strong>
              <p class="mb-0"><%= profile.mealsRequested %></p>
            </div>
            <% } %>
            <% if (profile.favoriteCategories) { %>
            <div class="mb-3">
              <strong>Favorite Categories:</strong>
              <p class="mb-0"><%= profile.favoriteCategories %></p>
            </div>
            <% } %>
            <% if (profile.complexity) { %>
            <div class="mb-3">
              <strong>Recipe Complexity:</strong>
              <p class="mb-0"><%= profile.complexity %>/5</p>
            </div>
            <% } %>
            <% if (profile.kitchenTools) { %>
            <div class="mb-3">
              <strong>Kitchen Tools & Pans:</strong>
              <p class="mb-0"><%= profile.kitchenTools %></p>
            </div>
            <% } %>
            <% if (profile.kitchenAppliances) { %>
            <div class="mb-3">
              <strong>Kitchen Appliances & Utilities:</strong>
              <p class="mb-0"><%= profile.kitchenAppliances %></p>
            </div>
            <% } %>
            <% if (profile.cookingNotes) { %>
            <div class="mb-3">
              <strong>Cooking Preferences & Notes:</strong>
              <p class="mb-0"><%= profile.cookingNotes %></p>
            </div>
            <% } %>
          </div>
        </div>
        <% } %>
      </div>
    </div>
  </div>

  <footer class="text-center mt-5 mb-4 text-muted small">
    Built with ❤️ using Node.js, Express, and EJS
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    function updateComplexityValue(value) {
      document.getElementById('complexity-value').textContent = '(' + value + ')';
    }
  </script>
</body>
</html>
